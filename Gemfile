source "https://rubygems.org"

# Specify your gem's dependencies in faa_auth.gemspec
gemspec

# add develpment dependencies
group :development do
  gem "debug"
end

group :development do
  eval_gemfile "gemfiles/rubocop.gemfile"
end
